'use strict';

cs247App.controller('RegisterController', ['$scope',
  function ($scope) {
    /*
     *
     */
     $scope.main = {};    

     /* assemble a json object with each click */
     
  }]);
